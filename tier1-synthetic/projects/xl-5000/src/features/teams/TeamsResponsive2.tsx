import { useState, useCallback, useMemo } from 'react';
import clsx from 'clsx';
import TeamsBadge from './TeamsBadge';
import TeamsResponsive1 from './TeamsResponsive1';
import TeamsAvatar from './TeamsAvatar';
import CatalogBarChart from '../catalog/CatalogBarChart';
import AuthAreaChart1 from '../auth/AuthAreaChart1';
import styles from './TeamsResponsive2.module.css';

type LayoutDirection = 'horizontal' | 'vertical';
type LayoutGap = 'none' | 'sm' | 'md' | 'lg' | 'xl';

interface TeamsResponsive2Props {
  children?: React.ReactNode;
  direction?: LayoutDirection;
  gap?: LayoutGap;
  padding?: LayoutGap;
  align?: 'start' | 'center' | 'end' | 'stretch';
  justify?: 'start' | 'center' | 'end' | 'between' | 'around';
  wrap?: boolean;
  fullWidth?: boolean;
  className?: string;
  as?: 'div' | 'section' | 'main' | 'aside' | 'article';

}

export default function TeamsResponsive2({
  children,
  direction = 'vertical',
  gap = 'md',
  padding = 'md',
  align = 'stretch',
  justify = 'start',
  wrap = false,
  fullWidth = false,
  className,
  as: Component = 'div',

}: TeamsResponsive2Props) {
  const [isVisible, setIsVisible] = useState(true);

  const toggleVisibility = useCallback(() => {
    setIsVisible(prev => !prev);
  }, []);

  const containerClasses = useMemo(
    () =>
      clsx(
        styles.container,
        styles[`dir-${direction}`],
        styles[`gap-${gap}`],
        styles[`pad-${padding}`],
        styles[`align-${align}`],
        styles[`justify-${justify}`],
        wrap && styles.wrap,
        fullWidth && styles.fullWidth,
        className
      ),
    [direction, gap, padding, align, justify, wrap, fullWidth, className]
  );

  return (
    <Component className={containerClasses}>
      {isVisible && (
        <div className={styles.content}>
          {children}
      <TeamsBadge />
      <TeamsResponsive1 />
      <TeamsAvatar />
      <CatalogBarChart />
      <AuthAreaChart1 />
        </div>
      )}
    </Component>
  );
}
